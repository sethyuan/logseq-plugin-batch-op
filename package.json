{
  "name": "logseq-plugin-batch-op",
  "version": "0.1.0",
  "main": "dist/index.html",
  "logseq": {
    "id": "_sethyuan-logseq-batch-op",
    "icon": "./icon.png"
  },
  "browserslist": [
    "last 1 version"
  ],
  "scripts": {
    "clean": "rm -rf dist",
    "dev": "mkdir -p dist && cp index-dev.html dist/index.html && preact watch --no-prerender --no-sw",
    "build": "preact build --no-prerender --no-sw --no-inline-css --no-esm --dest dist"
  },
  "dependencies": {
    "@logseq/libs": "^0.0.6",
    "antd": "^4.21.0",
    "logseq-l10n": "^0.1.0",
    "preact": "^10.7.3",
    "preact-render-to-string": "^5.2.0"
  },
  "devDependencies": {
    "preact-cli": "^3.3.5"
  }
}
